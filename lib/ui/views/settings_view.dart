import 'package:flutter/material.dart';

class SettingsViewModel extends StatelessWidget {
  const SettingsViewModel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
